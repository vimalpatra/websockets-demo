<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta http-equiv="X-UA-Compatible" content="IE=edge">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>Document</title>
	<script>
		const ROOM_ID = "<%= roomId %>";
	</script>
	<script src="https://unpkg.com/peerjs@1.4.5/dist/peerjs.min.js"></script>
	<script src="/socket.io/socket.io.js" defer></script>
	<script src="script.js" defer></script>
	<style>
		body {
			width: calc(100% - 260px);
    		margin: 180px auto;
		}
		header {
			padding: 20px 50px;
			width: 100%;
			position: absolute;
			right: 0;
			top: 0;
			text-align: right;
		}
		#video-grid {
			display: grid;
			place-content: center;
			grid-template-columns: repeat(auto-fill, 400px);
			grid-auto-rows: 225px;
			gap: 40px;
			/* grid-template-columns: repeat(auto-fit, 300px);
			grid-auto-rows: 300px; */
		}

		video {
			width: 100%;
			height: 100%;
			border: 10px solid hsl(0, 0%, 95%);
			border-radius: 5px;
			object-fit: cover;
		}

		.self-video {
			width: 200px;
			height: 112.5px;
			place-self: center;
			border: 10px solid hsla(0, 0%, 85%, 1);
			position: absolute;
			top: 30px;
    		left: 10px;
		}

		.self-video-wrapper {
			position: absolute;
			top: 10px;
			left: 10px;
		}
	</style>
</head>
<body>
	<header>
		<h3>Video Call using Peer to Peer connection through Websockets</h3>
	</header>
	<div class="self-video-wrapper">
		<span>You</span>
	</div>
	<!-- Video Grid -->
	<div id="video-grid">
		<video id="video" class="self-video"></video>
	</div>
</body>
</html>